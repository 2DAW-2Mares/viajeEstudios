Usuario:
  usuario{1..10}:
    nombre: "{{name.firstName}}"
    apellidos: "{{name.lastName}}"
    email: "{{internet.email}}"
    password: "{{internet.password}}"
    nre: "{{random.word}}"
    telefono: "{{phone.phoneNumber}}"
    curso: "{{random.word}}"

Centro:
  centro{1..5}:
    codigocentro: "{{random.number}}"
    nombre: "{{random.word}}"
    localidad: "{{address.country}}"
    verificado: "{{random.boolean}}"
    coordinador: "@usuario{@}"

Producto:
  producto{1..10}:
    descripcion: "{{lorem.sentence}}"
    referencia: "{{commerce.productName}}"
    precioVenta: "{{commerce.price}}"
    beneficio: "(function() { return Math.round(Math.random()*100);})()"

Objetivo:
  objetivo{1..15}:
    nombre: "{{random.word}}"
    fechaInicio: "{{date.past}}"
    fechaFin: "{{date.future}}"

Pedido:
  pedido{1..20}:
    fechaPedido: "{{date.past}}"

Proveedor:
  proveedor{1..4}:
    nombre: "{{random.word}}"

TipoProducto:
  tipoproducto{1..5}:
    nombre: "{{random.word}}"

Ingreso:
  ingreso{1..12}:
    cantidad: "{{random.number}}"
    verificado: "{{random.boolean}}"

DetallesPedido:
  detallespedido{1..10}:
    cantidadPedido: "{{random.number}}"
    cantidadEntrega: "{{random.number}}"
    pedidoId: "@pedido{@}"
    productoId: "@producto{@}"
